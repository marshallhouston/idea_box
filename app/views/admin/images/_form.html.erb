<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for [:admin, @image] do |f| %>

    <%= f.label :name %>
    <%= f.text_field :name %>

    <%= f.label :url %>
    <%= f.text_area :url %>

    <%= f.submit %>

    <% end %>
  </div>
</div>
