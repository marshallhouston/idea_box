<h3 class='headline'>All Images</h3>

<section class='images'>
  <% @images.each do |image|  %>
  <p><%= image.name %></p>
  <%= image_tag(image.url, size: "75x50", alt: image.name ) %>
  <p><%= link_to "Delete this image", admin_image_path(image), method: :delete %></p>
  <% end %>
</section>
